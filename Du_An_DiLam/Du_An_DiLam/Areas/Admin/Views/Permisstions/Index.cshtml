@model IEnumerable<Data.DataModel.Permisstion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Permisstion_Group.PermistionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Permisstion_Group.PermistionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PermisstionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PermisstionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PermisstionId })
        </td>
    </tr>
}

</table>
